<li>
  <%= gravatar_for user, :size => 30 %>
  <%= link_to user.name, user %>
  <%= user.admin %>
  <% if current_user.admin? %>
    <%= link_to "delete", user, :method => :delete, :confirm => "Sicuro?",
                                :title => "Delete #{user.name}" %>
  <% end %>
</li>

